# EMPETUR | Validação de Gabinete + Gestão de Campo

App em Streamlit para auditoria/validação de itens do inventário turístico.

## Rodar local
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows
pip install -r requirements.txt
streamlit run app.py
